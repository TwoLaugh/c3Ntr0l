import { AppShell } from "@/components/app-shell";

export default function SettingsPage() {
  return (
    <AppShell>
      <h1 className="page-title">Settings</h1>
      <div className="panel">
        <div className="row muted">Profile and planning settings.</div>
      </div>
    </AppShell>
  );
}
