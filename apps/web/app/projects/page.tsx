import { AppShell } from "@/components/app-shell";

export default function ProjectsPage() {
  return (
    <AppShell>
      <h1 className="page-title">Projects</h1>
      <div className="panel">
        <div className="row muted">Project admin surface.</div>
      </div>
    </AppShell>
  );
}
