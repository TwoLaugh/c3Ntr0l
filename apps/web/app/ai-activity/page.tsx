import { AppShell } from "@/components/app-shell";

export default function AIActivityPage() {
  return (
    <AppShell>
      <h1 className="page-title">AI Activity</h1>
      <div className="panel">
        <div className="row muted">AI action audit surface.</div>
      </div>
    </AppShell>
  );
}
